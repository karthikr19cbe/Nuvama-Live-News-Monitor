[project]
name = "nuvama-live-news-monitor"
version = "0.1.0"
description = "Financial news monitor with Telegram alerts and web dashboard"
requires-python = ">=3.11"
dependencies = [
    "flask>=3.1.2",
    "playwright>=1.55.0",
    "requests>=2.32.5",
    "python-dotenv>=1.0.0",
]
